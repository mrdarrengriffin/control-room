node_modules
dist
.astro
.git
.github
.devcontainer
.vscode
.idea
.env
.env.*
!.env.example
# Nothing from data/ is ever copied into an image — it is runtime state, mounted
# at run time. Keeping it out of the build context also means real credentials
# can't reach a published layer via the `COPY . .` in the dev and build stages.
data
npm-debug.log*
*.md
!README.md
